//====================================================================================================
//The Free Edition of Java to Python Converter limits conversion output to 100 lines per file.

//To purchase the Premium Edition, visit our website:
//https://www.tangiblesoftwaresolutions.com/order/order-java-to-python.html
//====================================================================================================

import math

class StageTracingDecoratorTest(Specification):
    def setup(self):
        listAppender.invokeMethod("clear", [])

    class ClosureAnonymousInnerClass23(Closure):

        def __init__(self, outerInstance):
            super().__init__(outerInstance, outerInstance)
            self.outerInstance = outerInstance

        def doCall(self, event, params):
            if event.getNum() == 1.asBoolean():
                npe = None
                npe.invokeMethod("trim", [])

            return [event]


    class ClosureAnonymousInnerClass24(Closure):

        def __init__(self, outerInstance):
            super().__init__(outerInstance, outerInstance)
            self.outerInstance = outerInstance

        def doCall(self, event, params):
            if event.getNum() == 2.asBoolean():
                assert False

            map1 = LinkedHashMap(1)
            map1.put("num", event.num)
            return [com.hpe.amce.translation.impl.OnParWithLegacyTest.TranslatedEvent(map1)]


    class ClosureAnonymousInnerClass25(Closure):

        def __init__(self, outerInstance):
            super().__init__(outerInstance, outerInstance)
            self.outerInstance = outerInstance

        def doCall(self, event, params):
            if event.getNum() == 3.asBoolean():
                (File("/ d o e s n o t e x i s t _ @ # ! \$ , :")).invokeMethod("getText", [])

            return [event]


    class ClosureAnonymousInnerClass26(Closure):

        def __init__(self, outerInstance):
            super().__init__(outerInstance, outerInstance)
            self.outerInstance = outerInstance

        def doCall(self, event, params):
            if math.fmod(event.getNum(), 2) == 0.asBoolean():
                raise NullPointerException("test exception that should be ignored")

            return [event]


    class ClosureAnonymousInnerClass27(Closure):

        def __init__(self, outerInstance):
            super().__init__(outerInstance, outerInstance)
            self.outerInstance = outerInstance

        def doCall(self, event, params):
            if math.fmod(event.getNum(), 2) != 0.asBoolean():
                raise NullPointerException("test exception that should be ignored")

            map1 = LinkedHashMap(1)
            map1.put("num", event.num)
            return [com.hpe.amce.translation.impl.OnParWithLegacyTest.TranslatedEvent(map1)]


    class ClosureAnonymousInnerClass28(Closure):

        def __init__(self, outerInstance):
            super().__init__(outerInstance, outerInstance)
            self.outerInstance = outerInstance

        def doCall(self, it):
            map1 = LinkedHashMap(1)
            map1.put("num", it)
            return com.hpe.amce.translation.impl.OnParWithLegacyTest.RawEvent(map1)

        def doCall(self):
            return doCall(None)


    class ClosureAnonymousInnerClass29(Closure):

        def __init__(self, outerInstance):
            super().__init__(outerInstance, outerInstance)
            self.outerInstance = outerInstance

        def doCall(self, raw, context):
            map1 = LinkedHashMap(1)
            map1.put("num", raw.num + 1)
            return [com.hpe.amce.translation.impl.OnParWithLegacyTest.RawEvent(map1)]


    class ClosureAnonymousInnerClass30(Closure):

        def __init__(self, outerInstance):
            super().__init__(outerInstance, outerInstance)
            self.outerInstance = outerInstance

        def doCall(self, raw, context):
            map1 = LinkedHashMap(1)
            map1.put("num", raw.num + 1)
            return [com.hpe.amce.translation.impl.OnParWithLegacyTest.RawEvent(map1)]


    class ClosureAnonymousInnerClass31(Closure):

        def __init__(self, outerInstance):
            super().__init__(outerInstance, outerInstance)
            self.outerInstance = outerInstance

        def doCall(self, raw, context):
            map1 = LinkedHashMap(1)
            map1.put("num", raw.num + 1)
            return [com.hpe.amce.translation.impl.OnParWithLegacyTest.TranslatedEvent(map1)]


    class ClosureAnonymousInnerClass32(Closure):

        def __init__(self, outerInstance):
            super().__init__(outerInstance, outerInstance)
            self.outerInstance = outerInstance

        def doCall(self, it):
            return it.loggerName.invokeMethod("startsWith", [com.hpe.amce.translation.impl.OnParWithLegacyTest.class.name])

        def doCall(self):
            return doCall(None)


    class ClosureAnonymousInnerClass33(Closure):

        def __init__(self, outerInstance):
            super().__init__(outerInstance, outerInstance)
            self.outerInstance = outerInstance

        def doCall(self, it):
            return it.level == Level.INFO

        def doCall(self):
            return doCall(None)


    class ClosureAnonymousInnerClass34(Closure):


//====================================================================================================
//End of the allowed output for the Free Edition of Java to Python Converter.

//To purchase the Premium Edition, visit our website:
//https://www.tangiblesoftwaresolutions.com/order/order-java-to-python.html
//====================================================================================================
